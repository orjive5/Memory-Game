import React from 'react';

function Header() {
    return (
        <header>
            <h1>ART GALLERY MEMORY GAME</h1>
            <p>
                Rules: Get points for choosing a card, but don't choose the same
                card twice!
            </p>
        </header>
    );
}

export default Header;
